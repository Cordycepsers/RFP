# Example environment variables for scraper fallbacks
# Copy this file to .env and fill in values as needed.
# Do NOT commit your .env with secrets to version control.

# ---------------- Devex ----------------
# If you have a valid Devex session cookie value, set it here to bypass login
# and reduce anti-bot triggers. Leave empty if not available.
DEVEX_SESSION_COOKIE=

# Optional: fallback login if cookie not provided
DEVEX_USERNAME=
DEVEX_PASSWORD=

# Enable headless browser fallback for Devex if HTTP fetch is blocked (0 or 1)
DEVEX_USE_BROWSER=0

# ---------------- DevelopmentAid ----------------
# Enable headless browser fallback to render dynamic search pages (0 or 1)
DEVELOPMENTAID_USE_BROWSER=0

# ---------------- Network (informational) ----------------
# Network timeout seconds is configured via your application config file under
# network.timeout_seconds (default 30). Not read from .env by default.

